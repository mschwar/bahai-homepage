# Bahá’í Homepage H2B packet

The repository is already agent-first and the source-selector placeholder is already live.

This packet does **not** redo archaeology/H1/H2A.

It has two stages:

1. **H2B-A — contract only**: define collection/source schema, runtime semantics, migration, fixture strategy, and tests. Stop for human review.
2. **H2B-B — implementation**: only after a verified Garden export exists, wire the selector to Hidden Words + the Garden preview collection.

Ruhi remains blocked behind H2B plus its own provenance/rights research.
